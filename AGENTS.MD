# Porject repository instructions

Before working in this repository, read the applicable rule files in `.codex/rules/`.
These files are intentionally kept separate by concern and are the repository's
Codex-compatible replacement for the former Cursor rules.

Reusable workflows are stored in `.agents/skills/`. Use the matching skill when
its trigger applies.
